var gameOver = function () {
  levels.currentLevel = levels.gameOver;
  player.initLocation();
};
import rng from './components/rng';
// console.log(rng());

console.log(c);

if (module.hot) {
  module.hot.accept();
}
